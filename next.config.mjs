/** @type {import('next').NextConfig} */
const nextConfig = {
  // Görsel optimizasyonu için format desteği
  images: {
    formats: ['image/avif', 'image/webp'],
  },
};

export default nextConfig;

